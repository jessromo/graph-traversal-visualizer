import Node from "./Node";

const rows = 4;
const cols = 5;

function Graph() {
  function graph(rows, cols) {
    const grid = [];
    for (let i = 0; i < rows; i++) {
      const row = [];
      for (let j = 0; j < cols; j++) {

      }
    }
  }
}
